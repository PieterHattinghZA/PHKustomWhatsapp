name: .NET 10 Avalonia validation

on:
  pull_request:
    branches:
      - main
    paths:
      - 'src/PHKustom.ChatClient/**'
      - '.github/workflows/dotnet-avalonia.yml'
  push:
    branches:
      - agent/fix-gui-stability-and-exports
    paths:
      - 'src/PHKustom.ChatClient/**'
      - '.github/workflows/dotnet-avalonia.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    name: Restore, build and publish
    runs-on: windows-latest
    timeout-minutes: 20

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install .NET 10 SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'
          cache: true
          cache-dependency-path: src/PHKustom.ChatClient/PHKustom.ChatClient.csproj

      - name: Display .NET information
        run: dotnet --info

      - name: Restore
        run: dotnet restore src/PHKustom.ChatClient/PHKustom.ChatClient.csproj

      - name: Build
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force artifacts | Out-Null
          dotnet build src/PHKustom.ChatClient/PHKustom.ChatClient.csproj --configuration Release --no-restore --warnaserror 2>&1 | Tee-Object -FilePath artifacts/build.log
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      - name: Publish Windows x64
        shell: pwsh
        run: |
          dotnet publish src/PHKustom.ChatClient/PHKustom.ChatClient.csproj --configuration Release --runtime win-x64 --self-contained true -p:PublishSingleFile=true -p:DebugType=None -p:DebugSymbols=false --output artifacts/PHKustom.ChatClient-win-x64 2>&1 | Tee-Object -FilePath artifacts/publish.log
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      - name: Upload diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: PHKustom.ChatClient-diagnostics
          path: |
            artifacts/build.log
            artifacts/publish.log
          if-no-files-found: warn
          retention-days: 14

      - name: Upload Windows build
        uses: actions/upload-artifact@v4
        with:
          name: PHKustom.ChatClient-win-x64
          path: artifacts/PHKustom.ChatClient-win-x64
          if-no-files-found: error
          retention-days: 14
